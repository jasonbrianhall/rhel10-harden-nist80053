---
###############################################################################
# Auto-split from rhel10-remediation.yml (OpenSCAP RHEL 10 STIG playbook)
# Functional area: software_and_services
# Package install/removal, systemd service enable/disable/mask, fapolicyd, usbguard.
#
# 486 tasks total, 42 tagged `dangerous` (can lock you out / break
# the system -- e.g. root login restriction, account lockout, package
# removal, service disabling, mount option changes, SELinux enforcing, FIPS,
# GRUB changes). Each dangerous task also has a "# DANGEROUS:" comment above
# it in this file. Skip them with:
#   ansible-playbook software_and_services.yml --skip-tags dangerous
# Or run ONLY them with:
#   ansible-playbook software_and_services.yml --tags dangerous
###############################################################################
- hosts: all
  become: true
  become_method: sudo
  become_user: root

  vars:
    var_logind_session_timeout: '300'
    var_time_service_set_maxpoll: '16'
    var_tftpd_secure_directory: /var/lib/tftpboot
  tasks:
  - name: Gather the package facts
    package_facts:
      manager: auto
    tags:
    - CCE-90477-1
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_aide_installed

  - name: Ensure aide is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90477-1
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_aide_installed

  - name: Build and Test AIDE Database - Ensure AIDE Is Installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - aide
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86942-0
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_build_database
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Periodic Execution of AIDE - Ensure AIDE is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86738-2
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Periodic Execution of AIDE - Install cron
    ansible.builtin.package:
      name: cronie
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86738-2
    - CJIS-5.10.1.3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - PCI-DSS-Req-11.5
    - PCI-DSSv4-11.5.2
    - aide_periodic_cron_checking
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Notification of Post-AIDE Scan Details - Ensure AIDE is installed
    ansible.builtin.package:
      name: '{{ item }}'
      state: present
    with_items:
    - aide
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90177-7
    - NIST-800-53-CM-3(5)
    - NIST-800-53-CM-6(a)
    - aide_scan_notification
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Configure AIDE to Use FIPS 140-2 for Validating Hashes - Ensure aide is installed
    ansible.builtin.package:
      name: aide
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90260-1
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_use_fips_hashes
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - dangerous

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Configure AIDE to Use FIPS 140-2 for Validating Hashes - Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90260-1
    - NIST-800-171-3.13.11
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-7
    - NIST-800-53-SI-7(1)
    - aide_use_fips_hashes
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - dangerous

  - name: Ensure crypto-policies is installed
    ansible.builtin.package:
      name: crypto-policies
      state: present
    tags:
    - CCE-89668-8
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_crypto-policies_installed

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Remove the GDM Package Group: Ensure gdm is removed'
    ansible.builtin.package:
      name: gdm
      state: absent
    when: '"gdm" in ansible_facts.packages'
    tags:
    - CCE-88880-0
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_gdm_removed
    - dangerous

  - name: Ensure gnutls-utils is installed
    ansible.builtin.package:
      name: gnutls-utils
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90403-7
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_gnutls-utils_installed

  - name: Ensure nss-tools is installed
    ansible.builtin.package:
      name: nss-tools
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87829-8
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_nss-tools_installed

  - name: Ensure sequoia-sq is installed
    ansible.builtin.package:
      name: sequoia-sq
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86458-7
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - enable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_sequoia-sq_installed

  - name: Ensure subscription-manager is installed
    ansible.builtin.package:
      name: subscription-manager
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-88542-6
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_subscription-manager_installed

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Uninstall gssproxy Package: Ensure gssproxy is removed'
    ansible.builtin.package:
      name: gssproxy
      state: absent
    tags:
    - CCE-87596-3
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_gssproxy_removed
    - dangerous

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Uninstall tuned Package: Ensure tuned is removed'
    ansible.builtin.package:
      name: tuned
      state: absent
    tags:
    - CCE-87654-0
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_tuned_removed
    - dangerous

  - name: Ensure dnf Removes Previous Package Versions - Ensure DNF Removes Previous Package Versions
    ansible.builtin.lineinfile:
      dest: /etc/dnf/dnf.conf
      regexp: ^#?clean_requirements_on_remove
      line: clean_requirements_on_remove=1
      insertafter: \[main\]
      create: true
    when: ( not ( "kernel-core" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages and not "openshift-kubelet" in ansible_facts.packages and "ostree" in ansible_proc_cmdline ) and "dnf" in ansible_facts.packages )
    tags:
    - CCE-88515-2
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-11(a)
    - NIST-800-53-CM-11(b)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-2(6)
    - clean_components_post_updating
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure dnf-automatic to Install Available Updates Automatically
    community.general.ini_file:
      dest: /etc/dnf/automatic.conf
      section: commands
      option: apply_updates
      value: 'yes'
      create: true
    when: ( not ( "kernel-core" in ansible_facts.packages and "rpm-ostree" in ansible_facts.packages and "bootc" in ansible_facts.packages and not "openshift-kubelet" in ansible_facts.packages and "ostree" in ansible_proc_cmdline ) and not ( ansible_virtualization_type in ["docker", "lxc", "openvz", "podman", "container"] ) )
    tags:
    - CCE-86671-5
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-2(5)
    - NIST-800-53-SI-2(c)
    - dnf-automatic_apply_updates
    - low_complexity
    - medium_disruption
    - medium_severity
    - no_reboot_needed
    - unknown_strategy

  - name: 'Ensure Red Hat GPG Key Installed: Read permission of GPG key directory'
    ansible.builtin.stat:
      path: /etc/pki/rpm-gpg/
    register: gpg_key_directory_permission
    check_mode: false
    tags:
    - CCE-88256-3
    - CJIS-5.10.4.1
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy

  - name: 'Ensure Red Hat GPG Key Installed:  Read signatures in GPG key using sq'
    ansible.builtin.command: sq inspect /etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release
    changed_when: false
    failed_when: false
    check_mode: false
    register: sq_fingerprints
    when: ansible_distribution_version is version('10.1', '>=')
    tags:
    - CCE-88256-3
    - CJIS-5.10.4.1
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy

  - name: 'Ensure Red Hat GPG Key Installed: Read signatures in GPG key'
    ansible.builtin.command: gpg --show-keys --with-fingerprint --with-colons "/etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release"
    changed_when: false
    register: gpg_fingerprints
    failed_when: false
    check_mode: false
    when: ansible_distribution_version is version('10.1', '<')
    tags:
    - CCE-88256-3
    - CJIS-5.10.4.1
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy

  - name: 'Ensure Red Hat GPG Key Installed: Import RedHat GPG key'
    ansible.builtin.command: rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release
    tags:
    - CCE-88256-3
    - CJIS-5.10.4.1
    - NIST-800-171-3.4.8
    - NIST-800-53-CM-5(3)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-12
    - NIST-800-53-SC-12(3)
    - NIST-800-53-SI-7
    - PCI-DSS-Req-6.2
    - PCI-DSSv4-6.3
    - PCI-DSSv4-6.3.3
    - ensure_redhat_gpgkey_installed
    - high_severity
    - medium_complexity
    - medium_disruption
    - no_reboot_needed
    - restrict_strategy

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Disable debug-shell SystemD Service - Disable service debug-shell
    block:
    - name: Disable debug-shell SystemD Service - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable debug-shell SystemD Service - Ensure debug-shell.service is Masked
      ansible.builtin.systemd:
        name: debug-shell.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("debug-shell.service", multiline=True)
    - name: Unit Socket Exists - debug-shell.socket
      ansible.builtin.command: systemctl -q list-unit-files debug-shell.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable debug-shell SystemD Service - Disable Socket debug-shell
      ansible.builtin.systemd:
        name: debug-shell.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("debug-shell.socket", multiline=True)
    tags:
    - CCE-90212-2
    - NIST-800-171-3.4.5
    - NIST-800-53-CM-6
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_debug-shell_disabled
    - special_service_block
    - dangerous
    when: '"kernel-core" in ansible_facts.packages'

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Disable Ctrl-Alt-Del Burst Action
    ansible.builtin.lineinfile:
      dest: /etc/systemd/system.conf
      state: present
      regexp: ^CtrlAltDelBurstAction
      line: CtrlAltDelBurstAction=none
      create: true
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"systemd" in ansible_facts.packages'
    tags:
    - CCE-87627-6
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - disable_ctrlaltdel_burstaction
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - dangerous

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Disable Ctrl-Alt-Del Reboot Activation
    ansible.builtin.systemd:
      name: ctrl-alt-del.target
      force: true
      masked: true
      state: stopped
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90035-7
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-6(1)
    - NIST-800-53-CM-6(a)
    - disable_ctrlaltdel_reboot
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - dangerous

  - name: Remove StopIdleSessionSec from main config
    ansible.builtin.lineinfile:
      path: /etc/systemd/logind.conf
      regexp: ^\s*StopIdleSessionSec\s*=
      state: absent
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( ansible_distribution == 'RedHat' and ansible_distribution_version is version('8.7', '>=') and ansible_distribution == 'RedHat' and ansible_distribution_version is version('9.0', '!=') ) or ansible_distribution == 'OracleLinux' and ansible_distribution_version is version('8.7', '>=') or ansible_distribution == 'SLES' and ansible_distribution_version is version('15', '>=')
    tags:
    - CCE-88334-8
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - logind_session_timeout
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy

  - name: Remove StopIdleSessionSec from drop-in files
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: ^\s*StopIdleSessionSec\s*=
      state: absent
    with_fileglob:
    - /etc/systemd/logind.conf.d/*.conf
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( ansible_distribution == 'RedHat' and ansible_distribution_version is version('8.7', '>=') and ansible_distribution == 'RedHat' and ansible_distribution_version is version('9.0', '!=') ) or ansible_distribution == 'OracleLinux' and ansible_distribution_version is version('8.7', '>=') or ansible_distribution == 'SLES' and ansible_distribution_version is version('15', '>=')
    tags:
    - CCE-88334-8
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - logind_session_timeout
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy

  - name: Set 'StopIdleSessionSec' to '{{ var_logind_session_timeout }}' in the [Login] section of '/etc/systemd/logind.conf.d/oscap-idle-sessions.conf'
    community.general.ini_file:
      path: /etc/systemd/logind.conf.d/oscap-idle-sessions.conf
      section: Login
      option: StopIdleSessionSec
      value: '{{ var_logind_session_timeout }}'
      create: true
      mode: 420
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( ansible_distribution == 'RedHat' and ansible_distribution_version is version('8.7', '>=') and ansible_distribution == 'RedHat' and ansible_distribution_version is version('9.0', '!=') ) or ansible_distribution == 'OracleLinux' and ansible_distribution_version is version('8.7', '>=') or ansible_distribution == 'SLES' and ansible_distribution_version is version('15', '>=')
    tags:
    - CCE-88334-8
    - CJIS-5.5.6
    - NIST-800-171-3.1.11
    - NIST-800-53-AC-12
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-17(a)
    - NIST-800-53-AC-2(5)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SC-10
    - PCI-DSS-Req-8.1.8
    - logind_session_timeout
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy

  - name: Require Authentication for Single User Mode - find files which already override Execstart of rescue.service
    ansible.builtin.find:
      paths: /etc/systemd/system/rescue.service.d
      patterns: '*.conf'
      contains: ^\s*ExecStart=.*$
    register: rescue_service_overrides_found
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90014-2
    - NIST-800-171-3.1.1
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-2
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - require_singleuser_auth
    - restrict_strategy

  - name: Require Authentication for Single User Mode - set default target for rescue.service override
    ansible.builtin.set_fact:
      rescue_service_remediation_target_file:
      - /etc/systemd/system/rescue.service.d/10-oscap.conf
    when:
    - '"kernel-core" in ansible_facts.packages'
    - rescue_service_overrides_found.matched is defined and rescue_service_overrides_found.matched == 0
    tags:
    - CCE-90014-2
    - NIST-800-171-3.1.1
    - NIST-800-171-3.4.5
    - NIST-800-53-AC-3
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-2
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - require_singleuser_auth
    - restrict_strategy

  - name: Ensure opensc is installed
    ansible.builtin.package:
      name: opensc
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86898-4
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_opensc_installed

  - name: Ensure pcsc-lite-ccid is installed
    ansible.builtin.package:
      name: pcsc-lite-ccid
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86250-8
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_pcsc-lite-ccid_installed

  - name: Ensure pcsc-lite is installed
    ansible.builtin.package:
      name: pcsc-lite
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-88682-0
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_pcsc-lite_installed

  - name: Ensure pkcs11-provider is installed
    ansible.builtin.package:
      name: pkcs11-provider
      state: present
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ansible_architecture != "s390x"
    tags:
    - CCE-86642-6
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-8.3
    - enable_strategy
    - install_smartcard_packages
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed

  - name: Enable the pcscd Service - Enable service pcscd
    block:
    - name: Enable the pcscd Service - Enable Service pcscd
      ansible.builtin.systemd:
        name: pcscd
        enabled: true
        state: started
        masked: false
      when:
      - '"pcsc-lite" in ansible_facts.packages'
    tags:
    - CCE-87363-8
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-2(1)
    - NIST-800-53-IA-2(11)
    - NIST-800-53-IA-2(2)
    - NIST-800-53-IA-2(3)
    - NIST-800-53-IA-2(4)
    - NIST-800-53-IA-2(6)
    - NIST-800-53-IA-2(7)
    - PCI-DSS-Req-8.3
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_pcscd_enabled
    - special_service_block
    when: '"kernel-core" in ansible_facts.packages'

  - name: Ensure libreswan is installed
    ansible.builtin.package:
      name: libreswan
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87497-4
    - NIST-800-53-CM-6(a)
    - PCI-DSS-Req-4.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_libreswan_installed

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Disable the Automounter - Disable service autofs
    block:
    - name: Disable the Automounter - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the Automounter - Ensure autofs.service is Masked
      ansible.builtin.systemd:
        name: autofs.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("autofs.service", multiline=True)
    - name: Unit Socket Exists - autofs.socket
      ansible.builtin.command: systemctl -q list-unit-files autofs.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable the Automounter - Disable Socket autofs
      ansible.builtin.systemd:
        name: autofs.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("autofs.socket", multiline=True)
    tags:
    - CCE-88947-7
    - NIST-800-171-3.4.6
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-MP-7
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_autofs_disabled
    - special_service_block
    - dangerous
    when: ( "autofs" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )

  - name: Ensure policycoreutils-python-utils is installed
    ansible.builtin.package:
      name: policycoreutils-python-utils
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-87004-8
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_policycoreutils-python-utils_installed

  - name: Ensure policycoreutils is installed
    ansible.builtin.package:
      name: policycoreutils
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-88996-4
    - enable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_policycoreutils_installed

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: Disable KDump Kernel Crash Analyzer (kdump) - Disable service kdump
    block:
    - name: Disable KDump Kernel Crash Analyzer (kdump) - Collect systemd Services Present in the System
      ansible.builtin.command: systemctl -q list-unit-files --type service
      register: service_exists
      changed_when: false
      failed_when: service_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable KDump Kernel Crash Analyzer (kdump) - Ensure kdump.service is Masked
      ansible.builtin.systemd:
        name: kdump.service
        state: stopped
        enabled: false
        masked: true
      when: service_exists.stdout_lines is search("kdump.service", multiline=True)
    - name: Unit Socket Exists - kdump.socket
      ansible.builtin.command: systemctl -q list-unit-files kdump.socket
      register: socket_file_exists
      changed_when: false
      failed_when: socket_file_exists.rc not in [0, 1]
      check_mode: false
    - name: Disable KDump Kernel Crash Analyzer (kdump) - Disable Socket kdump
      ansible.builtin.systemd:
        name: kdump.socket
        enabled: false
        state: stopped
        masked: true
      when: socket_file_exists.stdout_lines is search("kdump.socket", multiline=True)
    tags:
    - CCE-88407-2
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_kdump_disabled
    - special_service_block
    - dangerous
    when: '"kernel-core" in ansible_facts.packages'

  - name: Ensure cronie is installed
    ansible.builtin.package:
      name: cronie
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86619-4
    - NIST-800-53-CM-6(a)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.6
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_cron_installed

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Uninstall unbound Package: Ensure unbound is removed'
    ansible.builtin.package:
      name: unbound
      state: absent
    tags:
    - CCE-86181-5
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_unbound_removed
    - dangerous

  - name: Ensure fapolicyd is installed
    ansible.builtin.package:
      name: fapolicyd
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-89813-0
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-4(22)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_fapolicyd_installed

  - name: Enable the File Access Policy Service - Enable service fapolicyd
    block:
    - name: Enable the File Access Policy Service - Enable Service fapolicyd
      ansible.builtin.systemd:
        name: fapolicyd
        enabled: false
        masked: false
      when:
      - '"fapolicyd" in ansible_facts.packages'
    tags:
    - CCE-88147-4
    - NIST-800-53-CM-6(a)
    - NIST-800-53-SI-4(22)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_fapolicyd_enabled
    - special_service_block
    when: '"kernel-core" in ansible_facts.packages'

  - name: Configure Fapolicy Module to Employ a Deny-all, Permit-by-exception Policy to Allow the Execution of Authorized Software Programs. - Gather the package facts
    ansible.builtin.package_facts:
      manager: auto
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-90343-5
    - NIST-800-53-CM-6 b
    - NIST-800-53-CM-7 (2)
    - NIST-800-53-CM-7 (5) (b)
    - fapolicy_default_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Fapolicy Module to Employ a Deny-all, Permit-by-exception Policy to Allow the Execution of Authorized Software Programs. - Ensure a Final Rule Denying Everything
    ansible.builtin.copy:
      content: '# Red Hat KCS 7003854 (https://access.redhat.com/solutions/7003854)

        deny perm=any all : all

        '
      dest: /etc/fapolicyd/rules.d/99-deny-everything.rules
      owner: root
      group: fapolicyd
      mode: '0644'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"fapolicyd" in ansible_facts.packages'
    register: result_fapolicyd_final_rule
    tags:
    - CCE-90343-5
    - NIST-800-53-CM-6 b
    - NIST-800-53-CM-7 (2)
    - NIST-800-53-CM-7 (5) (b)
    - fapolicy_default_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Fapolicy Module to Employ a Deny-all, Permit-by-exception Policy to Allow the Execution of Authorized Software Programs. - Ensure fapolicyd is Not Permissive
    ansible.builtin.lineinfile:
      path: /etc/fapolicyd/fapolicyd.conf
      regexp: ^(permissive\s*=).*$
      line: \1 0
      backrefs: true
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"fapolicyd" in ansible_facts.packages'
    register: result_fapolicyd_enforced
    tags:
    - CCE-90343-5
    - NIST-800-53-CM-6 b
    - NIST-800-53-CM-7 (2)
    - NIST-800-53-CM-7 (5) (b)
    - fapolicy_default_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Fapolicy Module to Employ a Deny-all, Permit-by-exception Policy to Allow the Execution of Authorized Software Programs. - Restart fapolicyd If Permissive Mode or Final Rule is Changed
    ansible.builtin.service:
      name: fapolicyd
      state: restarted
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"fapolicyd" in ansible_facts.packages'
    - result_fapolicyd_final_rule is changed or result_fapolicyd_enforced is changed
    tags:
    - CCE-90343-5
    - NIST-800-53-CM-6 b
    - NIST-800-53-CM-7 (2)
    - NIST-800-53-CM-7 (5) (b)
    - fapolicy_default_deny
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Uninstall vsftpd Package: Ensure vsftpd is removed'
    ansible.builtin.package:
      name: vsftpd
      state: absent
    tags:
    - CCE-88674-7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - NIST-800-53-CM-7.1(ii)
    - NIST-800-53-IA-5(1)(c)
    - NIST-800-53-IA-5(1).1(v)
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_vsftpd_removed
    - dangerous

  - name: Ensure postfix is installed
    ansible.builtin.package:
      name: postfix
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86466-0
    - PCI-DSSv4-10.5
    - PCI-DSSv4-10.5.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_postfix_installed

  - name: Ensure s-nail is installed
    ansible.builtin.package:
      name: s-nail
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-89346-1
    - NIST-800-53-CM-3(5)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_s-nail_installed

  - name: Ensure chrony is installed
    ansible.builtin.package:
      name: chrony
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-89591-2
    - PCI-DSS-Req-10.4
    - PCI-DSSv4-10.6
    - PCI-DSSv4-10.6.1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_chrony_installed

  - name: The Chronyd service is enabled - Enable service chronyd
    block:
    - name: The Chronyd service is enabled - Enable Service chronyd
      ansible.builtin.systemd:
        name: chronyd
        enabled: true
        state: started
        masked: false
      when:
      - '"chrony" in ansible_facts.packages'
    tags:
    - CCE-90511-7
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_chronyd_enabled
    - special_service_block
    when:
    - '"kernel-core" in ansible_facts.packages'
    - '"chrony" in ansible_facts.packages'

  - name: Configure Time Service Maxpoll Interval - Check That /etc/ntp.conf Exist
    ansible.builtin.stat:
      path: /etc/ntp.conf
    register: ntp_conf_exist_result
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Update the maxpoll Values in /etc/ntp.conf
    ansible.builtin.replace:
      path: /etc/ntp.conf
      regexp: ^(server.*maxpoll)[ ]+[0-9]+(.*)$
      replace: \1 {{ var_time_service_set_maxpoll }}\2
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - ntp_conf_exist_result.stat.exists
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Set the maxpoll Values in /etc/ntp.conf
    ansible.builtin.replace:
      path: /etc/ntp.conf
      regexp: (^server\s+((?!maxpoll).)*)$
      replace: \1 maxpoll {{ var_time_service_set_maxpoll }}\n
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - ntp_conf_exist_result.stat.exists
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Check That /etc/chrony.conf Exist
    ansible.builtin.stat:
      path: /etc/chrony.conf
    register: chrony_conf_exist_result
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Update the maxpoll Values in /etc/chrony.conf
    ansible.builtin.replace:
      path: /etc/chrony.conf
      regexp: ^((?:server|pool|peer).*maxpoll)[ ]+[0-9]+(.*)$
      replace: \1 {{ var_time_service_set_maxpoll }}\2
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - chrony_conf_exist_result.stat.exists
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Set the maxpoll Values in /etc/chrony.conf
    ansible.builtin.replace:
      path: /etc/chrony.conf
      regexp: (^(?:server|pool|peer)\s+((?!maxpoll).)*)$
      replace: \1 maxpoll {{ var_time_service_set_maxpoll }}\n
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - chrony_conf_exist_result.stat.exists
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Check That /etc/chrony.d/ Exist
    ansible.builtin.stat:
      path: /etc/chrony.d/
    register: chrony_d_path_exists
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Get Conf Files from /etc/chrony.d/
    ansible.builtin.find:
      path: /etc/chrony.d/
      patterns: '*.conf'
      file_type: file
    register: chrony_d_conf_files
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - chrony_d_path_exists.stat.exists and chrony_d_path_exists.stat.isdir
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Update the maxpoll Values in /etc/chrony.d/
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: ^((?:server|pool|peer).*maxpoll)[ ]+[0-9,-]+(.*)$
      replace: \1 {{ var_time_service_set_maxpoll }}\2
    loop: '{{ chrony_d_conf_files.files | default([]) }}'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - chrony_d_conf_files is defined and chrony_d_conf_files.matched
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  - name: Configure Time Service Maxpoll Interval - Set the maxpoll Values in /etc/chrony.d/
    ansible.builtin.replace:
      path: '{{ item.path }}'
      regexp: (^(?:server|pool|peer)\s+((?!maxpoll).)*)$
      replace: \1 maxpoll {{ var_time_service_set_maxpoll }}\n
    loop: '{{ chrony_d_conf_files.files | default([]) }}'
    when:
    - '"kernel-core" in ansible_facts.packages'
    - ( "chrony" in ansible_facts.packages or "ntp" in ansible_facts.packages )
    - chrony_d_conf_files is defined and chrony_d_conf_files.matched
    tags:
    - CCE-88549-1
    - NIST-800-53-AU-12(1)
    - NIST-800-53-AU-8(1)(b)
    - NIST-800-53-CM-6(a)
    - chronyd_or_ntpd_set_maxpoll
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - restrict_strategy

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Uninstall telnet-server Package: Ensure telnet-server is removed'
    ansible.builtin.package:
      name: telnet-server
      state: absent
    tags:
    - CCE-88105-2
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSS-Req-2.2.2
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_telnet-server_removed
    - dangerous

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Uninstall tftp-server Package: Ensure tftp-server is removed'
    ansible.builtin.package:
      name: tftp-server
      state: absent
    tags:
    - CCE-89287-7
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - high_severity
    - low_complexity
    - low_disruption
    - no_reboot_needed
    - package_tftp-server_removed
    - dangerous

  # DANGEROUS: this task can lock out access, remove software,
  # disable a service, or otherwise change system behavior in a
  # breaking way. Review before running.
  - name: 'Remove tftp Daemon: Ensure tftp is removed'
    ansible.builtin.package:
      name: tftp
      state: absent
    tags:
    - CCE-88586-3
    - PCI-DSSv4-2.2
    - PCI-DSSv4-2.2.4
    - disable_strategy
    - low_complexity
    - low_disruption
    - low_severity
    - no_reboot_needed
    - package_tftp_removed
    - dangerous

  - name: Ensure tftp systemd Service Uses Secure Mode - Find valid drop-ins
    ansible.builtin.find:
      paths: /etc/systemd/system/tftp.service.d
      patterns: '*.conf'
      contains: ^\s*ExecStart\s*=\s*/\S+\s+-s\s+/\S+$
    register: valid_dropins
    failed_when: false
    when: '"tftp-server" in ansible_facts.packages'
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Find all drop-in files
    ansible.builtin.find:
      paths: /etc/systemd/system/tftp.service.d
      patterns: '*.conf'
      contains: ^\s*ExecStart\s*=.*$
      file_type: file
    register: all_dropins
    failed_when: false
    when: '"tftp-server" in ansible_facts.packages'
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Get invalid drop-ins
    ansible.builtin.set_fact:
      invalid_dropins: '{{ all_dropins.files | rejectattr(''path'', ''in'', valid_dropins.files | map(attribute=''path'') | list) | map(attribute=''path'') | list }}'
    when: '"tftp-server" in ansible_facts.packages'
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Comment all ExecStart in invalid drop-ins
    ansible.builtin.lineinfile:
      path: '{{ item }}'
      regexp: ^\s*ExecStart\s*=.*
      state: absent
    loop: '{{ invalid_dropins }}'
    when:
    - '"tftp-server" in ansible_facts.packages'
    - invalid_dropins | length > 0
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Check if a valid drop-in exists
    ansible.builtin.set_fact:
      tftp_config_valid: '{{ (valid_dropins.matched | default(0)) > 0 }}'
    when: '"tftp-server" in ansible_facts.packages'
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Check if tftp.service contains valid ExecStart
    ansible.builtin.find:
      paths: /usr/lib/systemd/system
      patterns: tftp.service
      contains: ^\s*ExecStart\s*=\s*/\S+\s+-s\s+/\S+
    register: valid_tftp_service
    when:
    - '"tftp-server" in ansible_facts.packages'
    - not tftp_config_valid
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Check if a valid tftp.service exists
    ansible.builtin.set_fact:
      original_valid: '{{ (valid_tftp_service.matched | default(0)) > 0 }}'
    when:
    - '"tftp-server" in ansible_facts.packages'
    - not tftp_config_valid
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Recalculate global config validity
    ansible.builtin.set_fact:
      tftp_config_valid: '{{ tftp_config_valid or original_valid | default(false) }}'
    when: '"tftp-server" in ansible_facts.packages'
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure tftp systemd Service Uses Secure Mode - Remediate only if necessary
    block:
    - name: Ensure drop-in directory exists
      ansible.builtin.file:
        path: /etc/systemd/system/tftp.service.d
        state: directory
    - name: Deploy 10-ssg-hardening.conf drop-in
      ansible.builtin.copy:
        dest: /etc/systemd/system/tftp.service.d/10-ssg-hardening.conf
        content: '[Service]

          # clear any existing ExecStart in the original unit

          ExecStart=

          ExecStart=/usr/sbin/in.tftpd -s {{ var_tftpd_secure_directory }}'
    - name: Reload systemd and restart tftp service
      ansible.builtin.systemd:
        daemon_reload: true
        name: tftp
        state: restarted
        enabled: true
    when:
    - '"tftp-server" in ansible_facts.packages'
    - not tftp_config_valid
    tags:
    - CCE-86495-9
    - NIST-800-53-IA-5 (1) (c)
    - configure_strategy
    - low_disruption
    - medium_complexity
    - medium_severity
    - no_reboot_needed
    - tftp_uses_secure_mode_systemd

  - name: Ensure openssh-clients is installed
    ansible.builtin.package:
      name: openssh-clients
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-86852-1
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_openssh-clients_installed

  - name: Ensure openssh-server is installed
    ansible.builtin.package:
      name: openssh-server
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-89241-4
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_openssh-server_installed

  - name: Ensure sssd is installed
    ansible.builtin.package:
      name: sssd
      state: present
    when: '"sssd-common" in ansible_facts.packages'
    tags:
    - CCE-88372-8
    - NIST-800-53-CM-6(a)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_sssd_installed

  - name: Enable the SSSD Service - Enable service sssd
    block:
    - name: Enable the SSSD Service - Enable Service sssd
      ansible.builtin.systemd:
        name: sssd
        enabled: true
        state: started
        masked: false
      when:
      - '"sssd-common" in ansible_facts.packages'
    tags:
    - CCE-87447-9
    - NIST-800-53-CM-6(a)
    - NIST-800-53-IA-5(10)
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_sssd_enabled
    - special_service_block
    when:
    - '"sssd-common" in ansible_facts.packages'
    - ( "sssd-common" in ansible_facts.packages and "kernel-core" in ansible_facts.packages )

  - name: Ensure usbguard is installed
    ansible.builtin.package:
      name: usbguard
      state: present
    when: ( ansible_architecture != "s390x" and "kernel-core" in ansible_facts.packages )
    tags:
    - CCE-87756-3
    - NIST-800-53-CM-8(3)
    - NIST-800-53-IA-3
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_usbguard_installed

  - name: Enable the USBGuard Service - Enable service usbguard
    block:
    - name: Enable the USBGuard Service - Enable Service usbguard
      ansible.builtin.systemd:
        name: usbguard
        enabled: true
        state: started
        masked: false
      when:
      - '"usbguard" in ansible_facts.packages'
    tags:
    - CCE-87560-9
    - NIST-800-53-CM-8(3)(a)
    - NIST-800-53-IA-3
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - service_usbguard_enabled
    - special_service_block
    when: ( ansible_architecture != "s390x" and "kernel-core" in ansible_facts.packages )

  - name: Generate USBGuard Policy
    block:
    - name: Check that the /etc/usbguard/rules.conf exists
      ansible.builtin.stat:
        path: /etc/usbguard/rules.conf
      register: policy_file
    - name: Create USBGuard Policy configuration
      ansible.builtin.command: usbguard generate-policy
      register: policy
      when: not policy_file.stat.exists or policy_file.stat.size == 0
    - name: Copy the Generated Policy configuration to a persistent file
      ansible.builtin.copy:
        content: '{{ policy.stdout }}'
        dest: /etc/usbguard/rules.conf
        mode: 384
      when: not policy_file.stat.exists or policy_file.stat.size == 0
    - name: Add comment into /etc/usbguard/rules.conf when system has no USB devices
      ansible.builtin.lineinfile:
        path: /etc/usbguard/rules.conf
        line: '# No USB devices found'
        state: present
      when: not policy_file.stat.exists or policy_file.stat.size == 0
    - name: Enable service usbguard
      ansible.builtin.systemd:
        name: usbguard
        enabled: 'yes'
        state: started
        masked: 'no'
    when:
    - ( ansible_architecture != "s390x" and "kernel-core" in ansible_facts.packages )
    - '"usbguard" in ansible_facts.packages'
    tags:
    - CCE-88632-5
    - NIST-800-53-CM-8(3)(a)
    - NIST-800-53-IA-3
    - configure_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - usbguard_generate_policy

  - name: Switch to multi-user runlevel
    ansible.builtin.file:
      src: /usr/lib/systemd/system/multi-user.target
      dest: /etc/systemd/system/default.target
      state: link
      force: true
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-89266-1
    - NIST-800-53-CM-6(a)
    - NIST-800-53-CM-7(a)
    - NIST-800-53-CM-7(b)
    - low_complexity
    - low_disruption
    - medium_severity
    - reboot_required
    - restrict_strategy
    - xwindows_runlevel_target

  - name: Ensure audispd-plugins is installed
    ansible.builtin.package:
      name: audispd-plugins
      state: present
    when: '"kernel-core" in ansible_facts.packages'
    tags:
    - CCE-88547-5
    - PCI-DSSv4-10.3
    - PCI-DSSv4-10.3.3
    - enable_strategy
    - low_complexity
    - low_disruption
    - medium_severity
    - no_reboot_needed
    - package_audispd-plugins_installed